import RouteComponent from "./shared/RouteComponent";
import Header from "./components/Header";

function App() {
  // const books = useSelector(state => state.books);
  // console.log(books);
  return (
    <>
      <Header />
      <RouteComponent />
    </>
  );
}

export default App;
